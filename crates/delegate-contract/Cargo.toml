[package]
name = "liberado-delegate-contract"
version = "0.1.0"
description = "Wire DTOs + route constants for LAN delegation: the one contract the delegator and the worker machine both speak."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[package.metadata.liberado]
# Layer role for the layer-rules test + crate-map generator (docs/architecture/contracts.md)
role = "client"
