[package]
name = "liberado-markdown"
version = "0.1.0"
description = "Lightweight Markdown parser for Liberado — UI-agnostic blocks and inline spans consumed by ratatui, Dioxus, and terminal output."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
