[package]
name = "liberado-provider-openai-compat"
version = "0.1.0"
description = "A single, config-driven Provider implementation for any OpenAI-compatible chat-completions API (DeepSeek, OpenRouter, and future backends like OpenAI direct/Groq/Together) — replaces what used to be one hand-copied crate per backend."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
liberado-provider = { workspace = true }
async-trait = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
