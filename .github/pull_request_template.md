## Backlog item

Item: `<id and name>`

## Still open

Confirmed by: `<git log, search, and code evidence>`

## Mutation evidence

Add one entry for each changed behavior. Confirm that the mutation applied before running the test.

### `<behavior>` — `<file:line>`

Broke it by: `<one-line change>`

```text
<test name and failure output>
```

Restored result: `<passing command and result>`

## Real-path evidence

`<How the test or verification drives the production path, or why that path cannot be driven>`

## Where it goes

`kernel | pack` — `<why a second pack would or would not need this>`

## Integration

- Base SHA: `<sha>`
- Predecessor: `<item or none>`
- Shared files: `<paths or none>`
- Merge order: `<after item or directly after main>`

## Not done

`<Any acceptance item not satisfied, and why>`
