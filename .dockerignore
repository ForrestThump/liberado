# Keep the build context small and the build reproducible. The Rust build needs only Cargo.toml /
# Cargo.lock / crates/ ; everything below is either huge, vendored, host-specific, or irrelevant.

# Build outputs (the biggest offender — never ship the host's target/).
target/
**/target/

# Vendored / separate projects that are NOT part of the workspace Rust build.
# turbovault/ and turbomcp/ leftover clones are gitignored path siblings. Cargo consumes those
# crates as git+tag pins (`liberado-2026-08-27`), so they must not enter the build context.
turbovault/
turbomcp/
liberado-pr-dispatch-mcp/
liberado-wakeup-mcp/
spider-mcp/
mcps/
**/.git/

# Git history and CI.
.git/
.github/

# Runtime state and local scratch (never bake host data into the image).
.liberado/
mutants.out/
mutants.out.old/

# Downloaded model caches (fastembed pulls an 87 MB ONNX model at runtime, not build time).
**/.fastembed_cache/

# Host WebUI build artifacts. Actions bakes a fresh bundle inside the builder; do not copy a host dist.
dist/
**/dist/
**/pkg/
node_modules/
**/node_modules/

# Docs and editor cruft — not needed to compile.
docs/
*.md
.vscode/
.idea/
