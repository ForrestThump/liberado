## Execution Contract

- **Done condition 1**: The file `crates/webui/src/components/chat.rs` contains a `<button>` element with the attribute `title="Send (Enter)"`.
- **Done condition 2**: No other files are modified.
- **Done condition 3**: The original Send button’s text (e.g., “Send”) is unchanged.