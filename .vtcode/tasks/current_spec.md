## Execution Spec

1. Identify the file `crates/webui/src/components/chat.rs`.
2. Locate the `<button>` element that serves as the Send button (likely contains the text “Send”).
3. Add a `title` attribute to that element with the exact value `"Send (Enter)"`.
4. Do not modify any other files or introduce any other changes.
5. Confirm the change by verifying the presence of `title="Send (Enter)"` on the correct element.